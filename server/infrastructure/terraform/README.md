# Terraform

This directory is a placeholder for production cloud infrastructure. The top-level modules represent the expected split:

- network
- database
- cache
- api runtime
- observability

Create the provider-specific module files when the target cloud account and deployment platform are known.
